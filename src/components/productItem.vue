<!--   <div class="row">
    <div class="col"></div>
    <div class="col"><q-checkbox size="sm" /></div>
    <div class="col">
      <q-img class="rounded-borders imgWidth" :src="imagesUrl?.imageUrl" />
    </div>
    <div class="col">{{ product.name }}</div>
    <div class="col">
      <q-badge
        size="15px"
        class="text-grey-10 text-weight-medium q-pa-sm"
        rounded
        :color="
          product.status === 'Active'
            ? 'light-green-2'
            : product.status === 'Draft'
            ? 'amber-1'
            : 'grey-2'
        "
        :label="product.status"
      />
    </div>
    <div class="col">
      <span>{{ product.stock }} in stock</span>
    </div>
    <div class="col">
      <span>{{ product.price }} EGP</span>
    </div>
    <div class="col">
      <span> {{ product.type }}</span>
    </div>
  </div> -->
